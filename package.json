{
  "name": "@stokly/imageremover",
  "version": "1.0.0",
  "description": "utility that works though a csv and replaces the images off the product with a blank array",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jacob Allen <j.allen@stok.ly>",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^2.1.0",
    "request": "^2.88.0"
  }
}
